# Glyphlex（PDF画像貼り付けアプリ）

PDFの上に画像や注釈を載せて、編集したPDFをダウンロードできるブラウザ向けツールです。  
インストール不要で、ローカルまたは GitHub Pages 上で動かします。

## できること

- **PDF読込** … 任意のPDFを開き、ページ単位でプレビュー
- **画像の貼り付け** … PNG / JPG を複数配置。ホームで先に選んでおくと、読込時に1ページ目へ自動配置
- **画像のみの編集** … PDFなしで画像を開き、透かし削除して画像のまま保存
- **画像の編集** … ドラッグで移動、ハンドルで拡大縮小（縦横比固定 / 幅のみ / 高さのみ）
- **透かしの軽減** … 選択中の画像、または画像専用画面の表示中画像に対して「画像の透かしを削除」を試せる
- **注釈ツール** … テキスト・図形・スタンプ（ツールバーから切替）
- **ズーム・ページ切替** … 表示拡大縮小と、サムネイルからのページ移動
- **保存** … 編集内容を埋め込んだPDFをダウンロード

## 起動方法

1. `pdf-app` フォルダで `start.bat` をダブルクリック
2. ブラウザで `http://localhost:8080` が開けばOK（自動で開かない場合は手動でアクセス）

初回は依存ライブラリの準備のため、少し時間がかかることがあります。  
終了するときは、黒い画面で `Ctrl + C` を押します。

必要なもの: **Node.js**（初回の `npm install` 用）、**Python**（ローカルHTTPサーバー用）

## 使い方の流れ

1. （任意）ホームの「画像の取り込み」で PNG / JPG を選ぶ
2. PDFなしで透かしを消す場合は「画像を開いて編集」→「画像の透かしを削除」→「画像を保存」
3. PDFを編集する場合はPDFを選び、「PDF読込」を押す
4. 編集画面で画像の追加・移動・リサイズ、必要なら透かし削除や注釈
5. 右上の「保存」で編集済みPDFをダウンロード

## 技術構成

| 項目 | 内容 |
|------|------|
| 形態 | 静的 HTML / CSS / JavaScript（ブラウザ完結） |
| PDF表示 | pdf.js |
| PDF書き出し | pdf-lib |
| ローカル起動 | `start.bat` → Python `http.server`（ポート 8080） |
| Web公開 | GitHub Pages（`npm run build:pages` で `docs` を生成） |

## 関連ファイル

- `README.md` … 操作手順・トラブルシュート・GitHub Pages 公開手順（詳細）
- `index.html` / `app.js` / `styles.css` … 画面と処理の本体
- `start.bat` … ローカル起動スクリプト

## 状態

安定しているユーティリティです。初心者でも使えるUIを想定しています。
